<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Designing_UserInterface" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.Designing_UserInterface" parent="Base.Theme.Designing_UserInterface" />

    <!-- Light mode -->
    <!-- Screen background if light mode on -->
    <style name="SCREEN">
        <item
            name="android:background">#ecf0f3</item>
    </style>
    <!-- Texts background if light mode on -->
    <style name="TEXT">
        <item name="android:textColor">#272424</item>
    </style>
    <!-- button background if light mode on -->
    <style name="BUTTON">
        <item
            name="android:background">#62A9C1</item>
        <item
            name="android:textColor">#FFF</item>
    </style>

</resources>