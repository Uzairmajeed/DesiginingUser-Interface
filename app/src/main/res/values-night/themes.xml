<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Designing_UserInterface" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <!-- DArk mode -->
    <!-- Screen background if light mode on -->
    <style name="SCREEN">
        <item
            name="android:background">#121010</item>
    </style>
    <!-- Texts background if Dark mode on -->
    <style name="TEXT">
        <item name="android:textColor">#FDFAFA</item>
    </style>
    <!-- button background if Dark mode on -->
    <style name="BUTTON">
        <item
            name="android:background">#095FEF</item>
        <item
            name="android:textColor">#FFF</item>
    </style>

</resources>